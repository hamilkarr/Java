

(접근 제어자) class 변수 {
  멤버 변수,
  메서드
}

접근 제어자의 종류
  publck : 외부클래스에서 접근 가능
  default : 동일 패키지 내에서만 접근 가능 (default 키워드는 생략 가능)
  protected : 외부 클래스에서 접근 불가능, 내부에서만 접근이 가능, 상속을 통해서만 내부에서 접근이 가능
  private : 내부에서만 접근이 가능


클래스 이름 짓는 규칙
  - 대문자로 시작(파스칼 케이스)
  - 